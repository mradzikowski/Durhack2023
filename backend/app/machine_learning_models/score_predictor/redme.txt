#use the following to install the packages one by one.

pip install pandas


pandas
scikit-learn
matplotlib
numpy


Then use, train.py to the model. For this case, the model has already been trained and changed to model.pkl file.

Then, use go to the directory, and type: python predict.py to see the output.
